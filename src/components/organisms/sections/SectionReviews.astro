---
import Card from '../../molecules/CardReview.astro';
---

<script>
	const sectionReviews = document.querySelectorAll('.sectionReviews-container__reviews');
	sectionReviews.forEach((element) => {
		element.addEventListener('mouseover', () => {
			element.style.animationPlayState = 'paused';
		});
		element.addEventListener('mouseleave', () => {
			element.style.animationPlayState = 'running';
		});
	});
</script>

<section class="sectionReviews">
	<div class="sectionTitle sectionReviews-title">
		<h2>Conheça os nossos <span class="gradient">Pacientes</span></h2>
		<p>E entenda como foram ajudados.</p>
	</div>

	<div class="sectionReviews-container">
		<ul class="sectionReviews-container__reviews">
			<li>
				<Card />
			</li>
			<li>
				<Card />
			</li>
			<li>
				<Card />
			</li>
		</ul>
		<div class="sectionReviews-container__fade"></div>
	</div>

	<div class="sectionReviews-container">
		<ul class="sectionReviews-container__reviews sectionReviews-container__reviews--reverse">
			<li>
				<Card />
			</li>
			<li>
				<Card />
			</li>
			<li>
				<Card />
			</li>
		</ul>
		<div class="sectionReviews-container__fade"></div>
	</div>
</section>

<style lang="scss">
	@import 'src/styles/_mixins.scss';

	.sectionReviews {
		padding-left: 0;
		padding-right: 0;
		padding-top: 0px;
		padding-bottom: 0px;
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		gap: 8px;
		overflow: hidden;
		margin-top: 80px;
		margin-bottom: 80px;

		@include tablet {
			padding-left: 0;
			padding-right: 0;
			margin-top: 70px;
			margin-bottom: 70px;
		}

		@include desktop {
			padding-left: 32px;
			padding-right: 32px;
			margin-top: 120px;
			margin-bottom: 120px;
			grid-template-columns: 1fr 1fr 1fr;
			gap: 25px;
			height: 830px;
			align-items: center;
		}

		&-title {
			padding-left: 32px;
			padding-right: 32px;

			@include tablet {
				padding-left: 70px;
				padding-right: 70px;
			}

			@include desktop {
				padding-left: 0;
				padding-right: 0;
				width: 400px;
				margin-bottom: 0px;
			}
		}

		&-container {
			position: relative;

			@include desktop {
				height: 830px;
			}

			&__reviews {
				display: flex;
				flex-direction: row;
				gap: 8px;
				margin: 0;
				animation: 20s slideHorizontal infinite linear;

				@include desktop {
					flex-direction: column;
					gap: 40px;
					animation: 20s slideVertical infinite linear;
				}

				&--reverse {
					animation: 20s slideHorizontalReverse infinite linear;
					@include desktop {
						animation: 20s slideVerticalReverse infinite linear;
					}
				}

				&--secondary {
					@include desktop {
						animation-delay: 10s;
					}
				}
			}

			&__fade {
				pointer-events: none;
				position: absolute;
				inset: 0;
				background: linear-gradient(90deg, white, transparent 20%, transparent 80%, white);

				@include desktop {
					background: linear-gradient(180deg, white, transparent 20%, transparent 80%, white);
				}
			}
		}

		@keyframes slideHorizontal {
			from {
				transform: translateX(0);
			}
			to {
				transform: translateX(-100%);
			}
		}

		@keyframes slideHorizontalReverse {
			from {
				transform: translateX(-100%);
			}
			to {
				transform: translateX(0);
			}
		}

		@keyframes slideVertical {
			from {
				transform: translateY(0);
			}
			to {
				transform: translateY(-100%);
			}
		}

		@keyframes slideVerticalReverse {
			from {
				transform: translateY(-100%);
			}
			to {
				transform: translateY(0);
			}
		}
	}
</style>
